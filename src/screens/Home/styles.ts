import styled, { css } from '@emotion/native';

export const StyledCardBody = styled.View`
  gap: 4px;
  padding: 12px;
`;

export const StyledSeparator = styled.View`
  height: 16px;
`;

export const contentContainerStyle = css`
  align-items: center;
  flex: 1;
  justify-content: center;
`;

export const StyledFloatingLabel = styled.View`
  bottom: 24px;
  position: absolute;
  right: 24px;
`;
